---
'@pasteltech/eslint-config-base': patch
---

Broaden `import/no-extraneous-dependencies` devDependencies allowlist to include `*.e2e.*`, `*.stories.*`, `*.mock.*`, `vitest.config.*`, and `jest.config.*` patterns.
