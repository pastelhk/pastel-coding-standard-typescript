---
'@pasteltech/eslint-config-base': minor
---

Add `@typescript-eslint/consistent-type-imports` rule (inline style) to enforce `import type` for type-only imports, aligning with `verbatimModuleSyntax` in tsconfig.
