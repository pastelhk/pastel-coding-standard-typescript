---
'@pasteltech/eslint-config-nextjs': patch
---

Add `loading` to the Next.js App Router default-export filename allowlist so `loading.tsx` files correctly receive the `import/prefer-default-export` override.
