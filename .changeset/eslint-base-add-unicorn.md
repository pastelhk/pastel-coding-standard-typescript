---
'@pasteltech/eslint-config-base': major
---

**BREAKING**: Add `eslint-plugin-unicorn` with curated rules (all errors). Code using `forEach`, bracket-notation index access, `/g` regex replace, etc. will now fail lint.
