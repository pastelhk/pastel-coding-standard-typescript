---
'@pasteltech/eslint-config-base': patch
---

Bump TypeScript peer dependency minimum from `>=5.6.0` to `>=5.8.0` to align with `verbatimModuleSyntax` and `erasableSyntaxOnly` requirements documented in the v2 changeset.
